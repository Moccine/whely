{% extends 'base.html.twig' %}
{% block body %}

    <section class="subpage-header">
        <div class="container">
            <div class="site-title clearfix">
                <h2>Inscription</h2>
                <ul class="breadcrumbs">
                    <li><a href="{{ path('home_page') }}">Accueil</a></li>
                    <li>inscription</li>
                </ul>
            </div>
        </div>
    </section>
    <!-- SERVICES CONTENT -->
    <section>
        <div class="container">
            <div class="row">
                {% if app.user %}
                    <h1> Vous êtes connecté</h1>
                {% else %}
                    <div class="container">
                        {{ form(form) }}
                    </div>
                    <div class="pull-left"><a href="{{ path('registration') }}">login</a> </div>

                {% endif %}
            </div>
    </section>
    <!-- / SERVICES CONTENT -->
    </div>
{% endblock %}

